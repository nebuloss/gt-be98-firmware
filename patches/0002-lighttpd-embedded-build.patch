diff --git a/release/src/router/lighttpd-1.4.39/src/fdevent_poll.c b/release/src/router/lighttpd-1.4.39/src/fdevent_poll.c
index e8dc385d50..f724ff156c 100755
--- a/release/src/router/lighttpd-1.4.39/src/fdevent_poll.c
+++ b/release/src/router/lighttpd-1.4.39/src/fdevent_poll.c
@@ -193,8 +193,8 @@ int fdevent_poll_init(fdevents *ev) {
 int fdevent_poll_init(fdevents *ev) {
 	UNUSED(ev);
 
-	log_error_write(srv, __FILE__, __LINE__,
-		"s", "poll is not supported, try to set server.event-handler = \"select\"");
+	fprintf(stderr,
+		"poll is not supported, try to set server.event-handler = \"select\"\n");
 
 	return -1;
 }
diff --git a/release/src/router/lighttpd-1.4.39/src/server.c b/release/src/router/lighttpd-1.4.39/src/server.c
index f95f2b3d9e..7f24329a05 100755
--- a/release/src/router/lighttpd-1.4.39/src/server.c
+++ b/release/src/router/lighttpd-1.4.39/src/server.c
@@ -232,6 +232,34 @@ static void signal_handler(int sig) {
 }
 #endif
 
+#if EMBEDDED_EANBLE
+/* ASUS embedded build uses -DEMBEDDED_EANBLE without autoconf HAVE_SIGACTION */
+static void embedded_signal_handler(int sig) {
+	switch (sig) {
+	case SIGTERM: srv_shutdown = 1; break;
+	case SIGINT:
+		if (graceful_shutdown) srv_shutdown = 1;
+		else graceful_shutdown = 1;
+		break;
+	case SIGALRM: handle_sig_alarm = 1; break;
+	case SIGHUP:  handle_sig_hup = 1; break;
+	case SIGCHLD: break;
+	case SIGUSR2: process_share_link_for_router_sync_use(); break;
+#if IM_MESSGAE_EANBLE
+	case IM_MSG_SIG_REQ: {
+		char *msg = (char *)malloc(1024);
+		read_im_msg_from_shm(&msg);
+		open_close_network_access_port(g_srv, 1);
+		write_im_resp_to_shm("OK");
+		free(msg);
+		break;
+	}
+#endif
+	default: break;
+	}
+}
+#endif
+
 #ifdef HAVE_FORK
 static void daemonize(void) {
 #ifdef SIGTTOU
@@ -1543,19 +1571,28 @@ int main (int argc, char **argv) {
 	sigaddset(&sigs_to_catch, SIGUSR2);   
 	
 	sigprocmask(SIG_UNBLOCK, &sigs_to_catch, NULL);
-	signal(SIGTERM, sigaction_handler);
-
-	//- 20121108 Sungmin add
-	signal(SIGUSR2, sigaction_handler);
-
+#if defined(HAVE_SIGACTION) && defined(SA_SIGINFO)
+	/* SIGTERM already registered via sigaction(); register ASUS extras */
+	sigaction(SIGUSR2, &act, NULL);
 #if IM_MESSGAE_EANBLE
-	signal(IM_MSG_SIG_REQ, sigaction_handler);	
+	sigaction(IM_MSG_SIG_REQ, &act, NULL);
+#endif
+#else
+	signal(SIGTERM, embedded_signal_handler);
+	signal(SIGUSR2, embedded_signal_handler);
+#if IM_MESSGAE_EANBLE
+	signal(IM_MSG_SIG_REQ, embedded_signal_handler);
+#endif
 #endif
 
 #endif
 
 #ifdef USE_ALARM
+#if defined(HAVE_SIGNAL) || defined(HAVE_SIGACTION)
 	signal(SIGALRM, signal_handler);
+#else
+	signal(SIGALRM, embedded_signal_handler);
+#endif
 
 	/* setup periodic timer (1 second) */
 	if (setitimer(ITIMER_REAL, &interval, NULL)) {
